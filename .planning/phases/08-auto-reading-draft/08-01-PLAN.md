---
phase: 08
plan: 01
title: Extraction Cache And Draft Configuration
status: planned
wave: 1
type: implementation
---

# Phase 08-01 Plan: Extraction Cache And Draft Configuration

<objective>
Add the local foundations required by `rsa note draft P###`: reading-draft config, extracted cache location, source selection helpers, local PDF/text extraction, prompt packet creation, and local-only safety rules.
</objective>

<read_first>
- `src/rsa_cli/config.py`
- `src/rsa_cli/skeleton.py`
- `src/rsa_cli/assets.py`
- `src/rsa_cli/notes.py`
- `.gitignore`
- `pyproject.toml`
- `.planning/phases/08-auto-reading-draft/08-CONTEXT.md`
</read_first>

<tasks>
<task id="08-01-T1" type="auto">
Add `reading_draft` config defaults and accessors, including non-sensitive LLM settings, review threshold, and `extracted_root`.
</task>

<task id="08-01-T2" type="auto">
Add local-only `01_literature/extracted/` skeleton and `.gitignore` coverage.
</task>

<task id="08-01-T3" type="auto">
Implement extraction/source-selection support for Phase 8. It must prefer source ledger PDF records, support `--source-file`, calculate source hash, write extraction metadata, and fail closed with Chinese diagnostics.
</task>
</tasks>

<acceptance_criteria>
- `ProjectConfig.extracted_root` resolves under `literature_root`.
- `create_literature_skeleton` creates `01_literature/extracted/.gitkeep`.
- `.gitignore` excludes extracted cache content while allowing `.gitkeep`.
- Missing/unusable sources do not create normal reading notes.
- Prompt packet and extraction metadata do not include API keys, cookies, credentials, or full LLM responses.
</acceptance_criteria>

<verification>
- `python -m pytest tests/test_config.py tests/test_skeleton.py tests/test_notes.py -q`
</verification>
