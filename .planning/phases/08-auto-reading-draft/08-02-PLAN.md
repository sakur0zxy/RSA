---
phase: 08
plan: 02
title: LLM Reading Draft And Review Packet
status: planned
wave: 1
type: implementation
---

# Phase 08-02 Plan: LLM Reading Draft And Review Packet

<objective>
Implement `rsa note draft P###` end to end: generate a structured Chinese reading note draft from extracted text, validate schema, run AI initial review, compute `agent_review_score_10`, and write a Chinese review packet with trace links.
</objective>

<read_first>
- `src/rsa_cli/notes.py`
- `src/rsa_cli/cli.py`
- `templates/paper_note.md`
- `src/rsa_cli/templates.py`
- `.planning/phases/08-auto-reading-draft/08-CONTEXT.md`
</read_first>

<tasks>
<task id="08-02-T1" type="auto">
Add reading draft rendering and validation fields while preserving existing `note create`, `note validate`, and `note status` behavior.
</task>

<task id="08-02-T2" type="auto">
Add deterministic local/mock LLM support for tests and fail-closed behavior for missing or unsupported real LLM configuration.
</task>

<task id="08-02-T3" type="auto">
Generate `P###_review_packet.md` with Chinese review guidance, links to metadata/source/note/cache/prompt packet, AI initial review score breakdown, and recommended human action.
</task>

<task id="08-02-T4" type="auto">
Add `rsa note draft P### [--source-file <path>] [--overwrite-draft] [--retry]` to CLI using existing argparse style and Chinese output.
</task>
</tasks>

<acceptance_criteria>
- Draft notes include stable English keys and Chinese explanations.
- `ready_for_review` requires `agent_review_score_10 >= 6` plus hard gates.
- `approved` and `human_confirmed: true` are never set by draft automation.
- Review packet links related local records and remains staging-only.
- CLI failures are Chinese and do not show tracebacks for expected user/config errors.
</acceptance_criteria>

<verification>
- `python -m pytest tests/test_notes.py tests/test_cli.py tests/test_templates.py -q`
</verification>
