---
phase: 05-evaluation-and-hardening
plan: "05-01"
wave: 1
subsystem: eval-fixtures-and-baseline
requirements_addressed: [EVAL-01]
files_modified:
  - src/rsa_cli/evals.py
  - src/rsa_cli/cli.py
  - src/rsa_cli/config.py
  - tests/test_evals.py
  - tests/test_cli.py
---

# Plan 05-01: Eval Fixtures And Scoring Rules

<objective>
Add deterministic local eval fixtures for harness regression risks and expose them through CLI commands.
</objective>

<must_haves>
- Add `rsa eval run` to execute all built-in fixtures and write `eval_report.md`.
- Add `rsa eval baseline` to write current fixture results as `eval_baseline.yaml`.
- Add `rsa eval compare` to compare current fixture results with baseline and write `eval_regression_report.md`.
- Fixtures must cover metadata hallucination, unauthorized PDF behavior, formal-record conflicts, output format drift and scope creep.
- Reports must include Chinese-readable detail and remediation guidance.
- No network calls, new dependencies or subagents.
</must_haves>

<tasks>
1. Add eval report and baseline paths to `ProjectConfig`.
2. Implement `src/rsa_cli/evals.py` with fixture runners, scoring and report rendering.
3. Add grouped `eval` CLI commands.
4. Add tests for passing fixtures, report writing, baseline writing and regression comparison.
</tasks>

<verification>
- `python -m pytest tests/test_evals.py tests/test_cli.py -q`
- `python -m pytest -q`
</verification>
