---
phase: "02-literature-records-pipeline"
plan: "03"
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - src/rsa_cli/index.py
  - src/rsa_cli/cli.py
  - src/rsa_cli/templates.py
  - 01_literature/paper_index.md
  - tests/test_index.py
  - tests/test_cli.py
autonomous: true
requirements: [META-02, META-03, META-01]
user_setup: []
must_haves:
  truths:
    - "META-02 and D-21, D-22, D-23, D-24, D-25 are implemented by generating and validating paper_index.md from metadata/P###.yaml, sorted by paper_id ascending, with explicit regenerate behavior."
    - "META-03 remains covered because index rows are derived from metadata fields including DOI/official URL, source reliability, PDF status, local paths, and last_checked even when only concise columns are displayed."
    - "META-01 and D-04, D-07 are preserved because index commands never create formal metadata and validate-index never modifies files."
    - "D-01, D-02, D-03 are implemented by Chinese index headings and field explanations while keeping English data keys in metadata."
  artifacts:
    - path: "src/rsa_cli/index.py"
      provides: "paper_index.md render, validate, and regenerate logic"
      contains: "render_paper_index"
    - path: "src/rsa_cli/cli.py"
      provides: "validate-index and regenerate-index subcommands"
      contains: "regenerate-index"
    - path: "tests/test_index.py"
      provides: "Index generation, stale mismatch, and explicit regenerate tests"
      min_lines: 80
    - path: "01_literature/paper_index.md"
      provides: "Human-readable generated index seed"
      contains: "文献索引"
  key_links:
    - from: "src/rsa_cli/index.py"
      to: "src/rsa_cli/metadata.py"
      via: "index reads validated metadata records"
      pattern: "load_metadata_record"
    - from: "src/rsa_cli/cli.py"
      to: "src/rsa_cli/index.py"
      via: "index subcommands call validate/regenerate functions"
      pattern: "regenerate_paper_index"
---

<objective>
Implement paper index generation and consistency checks.

Purpose: Make `paper_index.md` a readable Chinese index generated from formal metadata, not an independent source of truth, and make mismatches visible without automatic writes during validation.
Output: index module, CLI commands for `validate-index` and `regenerate-index`, generated index seed format, and tests for generation, sorting, mismatch detection, and explicit regeneration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-literature-records-pipeline/02-CONTEXT.md
@.planning/phases/02-literature-records-pipeline/02-RESEARCH.md
@.planning/phases/02-literature-records-pipeline/02-VALIDATION.md
@.planning/phases/02-literature-records-pipeline/02-PATTERNS.md
@.planning/phases/02-literature-records-pipeline/02-01-PLAN.md
@src/rsa_cli/metadata.py
@src/rsa_cli/cli.py
@01_literature/paper_index.md
@AGENTS.md
</context>

<threat_model>
- T-02-07 Index drift: `paper_index.md` can become stale or manually edited. Mitigate with deterministic render + compare validation that reports mismatches and exits nonzero.
- T-02-08 Hidden file writes during validation: validation can surprise the user by rewriting formal records. Mitigate by making `validate-index` read-only and requiring explicit `regenerate-index` for writes.
- T-02-09 Weak index evidence: concise display can hide source/PDF fields. Mitigate by deriving the index from validated metadata and including a Chinese note that metadata YAML is the source of truth.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic paper index renderer</name>
  <files>src/rsa_cli/index.py, src/rsa_cli/metadata.py, tests/test_index.py</files>
  <read_first>src/rsa_cli/metadata.py, .planning/phases/02-literature-records-pipeline/02-CONTEXT.md, .planning/phases/02-literature-records-pipeline/02-RESEARCH.md</read_first>
  <action>Create `src/rsa_cli/index.py` with `IndexError(ValueError)`, `collect_metadata_records(config: ProjectConfig) -> list[dict]`, and `render_paper_index(records: list[dict]) -> str`. It must load only `metadata/P###.yaml` files, sort records by `paper_id` ascending, and render Chinese Markdown headed `# 文献索引`. The table columns must be exactly `paper_id`, `year`, `title`, `venue`, `decision`, `used_for`, and `pdf_status`. Add a `## 字段说明` section that explains every column in Chinese and a source-of-truth sentence saying formal facts come from `metadata/P###.yaml`.</action>
  <verify>python -m pytest tests/test_index.py tests/test_metadata.py -q</verify>
  <acceptance_criteria>
    - `src/rsa_cli/index.py` contains `render_paper_index`.
    - Rendering records `P002` then `P001` outputs `P001` before `P002`.
    - Rendered Markdown contains `# 文献索引`, `字段说明`, and `metadata/P###.yaml`.
    - Rendered table contains every column named in the action text.
  </acceptance_criteria>
  <done>paper_index.md can be generated deterministically from metadata records.</done>
</task>

<task type="auto">
  <name>Task 2: Add validate/regenerate index functions and CLI commands</name>
  <files>src/rsa_cli/index.py, src/rsa_cli/cli.py, tests/test_index.py, tests/test_cli.py</files>
  <read_first>src/rsa_cli/index.py, src/rsa_cli/cli.py, tests/test_cli.py, .planning/phases/02-literature-records-pipeline/02-PATTERNS.md</read_first>
  <action>Implement `generate_paper_index(config: ProjectConfig) -> str`, `validate_paper_index(config: ProjectConfig) -> list[str]`, and `regenerate_paper_index(config: ProjectConfig) -> Path`. Add CLI commands `rsa validate-index` and `rsa regenerate-index`. `validate-index` must compare the current `config.paper_index_path` text against `generate_paper_index(config)`, print Chinese success text containing `索引一致` on match, and on mismatch print actionable Chinese errors containing `rsa regenerate-index`; it must not modify `paper_index.md`. `regenerate-index` must rewrite `paper_index.md` from metadata and print Chinese success text containing the output path.</action>
  <verify>python -m pytest tests/test_index.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `rsa --help` output contains `validate-index` and `regenerate-index`.
    - `rsa validate-index` detects a stale edited `paper_index.md` and leaves file contents unchanged.
    - `rsa regenerate-index` rewrites stale `paper_index.md` to the deterministic generated content.
    - Expected CLI errors contain no `Traceback`.
  </acceptance_criteria>
  <done>Users can detect and explicitly repair metadata/index divergence.</done>
</task>

<task type="auto">
  <name>Task 3: Seed generated index format and full Phase 2 smoke tests</name>
  <files>01_literature/paper_index.md, tests/test_index.py, tests/test_cli.py</files>
  <read_first>01_literature/paper_index.md, src/rsa_cli/index.py, .planning/phases/02-literature-records-pipeline/02-VALIDATION.md</read_first>
  <action>Update the committed `01_literature/paper_index.md` seed to the same generated empty-index format used by `render_paper_index([])`. Add full smoke tests in a temp project: run `rsa init`, run `rsa add-paper` with explicit human confirmation to create `P001`, run `rsa regenerate-index`, run `rsa validate-index`, then manually replace `paper_index.md` with stale text and prove `rsa validate-index` fails without rewriting it.</action>
  <verify>python -m pytest tests/test_index.py tests/test_cli.py tests/test_metadata.py tests/test_templates.py -q</verify>
  <acceptance_criteria>
    - Committed `01_literature/paper_index.md` contains `# 文献索引`.
    - Temp smoke test creates `metadata/P001.yaml`, `assets/P001/.gitkeep`, and an index row containing `P001`.
    - After stale manual edit, `validate-index` exits nonzero and the stale text remains unchanged.
    - Full Phase 2 quick test command exits 0.
  </acceptance_criteria>
  <done>Phase 2's metadata and index pipeline works end to end in a temp project.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_metadata.py tests/test_templates.py tests/test_index.py tests/test_cli.py -q`
- [ ] `python -m pytest -q`
- [ ] Temp smoke path: `rsa init`, `rsa add-paper ... --human-confirmed --confirmed-by zxy`, `rsa regenerate-index`, `rsa validate-index`.
- [ ] Confirm `validate-index` does not modify stale `paper_index.md`.
</verification>

<success_criteria>
- `paper_index.md` is readable, Chinese-explained, and consistent with metadata YAML records.
- The index is sorted by `paper_id` ascending.
- Validation reports mismatch details without mutating files.
- Regeneration is explicit and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-literature-records-pipeline/02-03-SUMMARY.md`
</output>
