---
phase: "02-literature-records-pipeline"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/rsa_cli/templates.py
  - templates/verification_review.md
  - templates/search_candidates.md
  - tests/test_templates.py
  - tests/test_cli.py
autonomous: true
requirements: [SRCH-01, SRCH-02, META-01]
user_setup: []
must_haves:
  truths:
    - "SRCH-01 and D-16, D-17 are implemented by keeping candidate review in per-round Markdown staging files, not formal metadata or C001 YAML records."
    - "SRCH-02 and D-18, D-19, D-20 are implemented by a verification_review.md table with verified/rejected/uncertain decisions, reasons, source evidence, and last_checked."
    - "META-01 and D-04, D-07, D-08 are preserved because verified candidates in review do not automatically write metadata/P###.yaml or consume formal IDs."
    - "D-01, D-02, D-03 are implemented by Chinese user-facing template text with English field names and Chinese explanations."
  artifacts:
    - path: "templates/verification_review.md"
      provides: "Candidate review staging template"
      contains: "candidate_title"
    - path: "src/rsa_cli/templates.py"
      provides: "Built-in verification review template"
      contains: "verification_review.md"
    - path: "tests/test_templates.py"
      provides: "Template localization and staging-boundary tests"
      min_lines: 40
  key_links:
    - from: "src/rsa_cli/templates.py"
      to: "templates/verification_review.md"
      via: "root template mirrors built-in fallback"
      pattern: "candidate_title"
    - from: "templates/verification_review.md"
      to: "src/rsa_cli/metadata.py"
      via: "template states verified is a suggestion until explicit add-paper"
      pattern: "human_confirmed"
---

<objective>
Implement candidate review staging templates for Phase 2.

Purpose: Give research rounds a lightweight, Chinese-explained `verification_review.md` format where candidates can be marked `verified`, `rejected`, or `uncertain` without entering formal metadata.
Output: localized review/search templates and tests proving review records stay staged and do not allocate `P###` IDs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-literature-records-pipeline/02-CONTEXT.md
@.planning/phases/02-literature-records-pipeline/02-RESEARCH.md
@.planning/phases/02-literature-records-pipeline/02-VALIDATION.md
@.planning/phases/02-literature-records-pipeline/02-PATTERNS.md
@.planning/phases/02-literature-records-pipeline/02-01-PLAN.md
@templates/verification_review.md
@templates/search_candidates.md
@src/rsa_cli/templates.py
@AGENTS.md
</context>

<threat_model>
- T-02-04 Candidate/formal boundary confusion: users may treat `verified` in review as formal intake. Mitigate with template text stating `verified` is only a source-check result and formal metadata still requires explicit `add-paper --human-confirmed`.
- T-02-05 Field-language ambiguity: English review keys may be unclear. Mitigate with Chinese field guide for every review field.
- T-02-06 Candidate ID creep: staging files can quietly create a second identifier scheme. Mitigate by explicitly avoiding `C001` files and by testing that review templates do not mention candidate YAML records.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Localize candidate review template with locked fields</name>
  <files>templates/verification_review.md, src/rsa_cli/templates.py, tests/test_templates.py</files>
  <read_first>templates/verification_review.md, src/rsa_cli/templates.py, .planning/phases/02-literature-records-pipeline/02-CONTEXT.md</read_first>
  <action>Replace the review table in `templates/verification_review.md` and `TEMPLATE_FILES[\"verification_review.md\"]` with a Chinese user-facing template headed `# 候选文献核验记录`. Include the exact English field names `candidate_title`, `source`, `doi_or_url`, `decision`, `reason`, and `last_checked` as table columns. Add a `## 字段说明` section that explains each field in Chinese. Add a `## 状态取值` section listing only `verified`, `rejected`, and `uncertain`, and state in Chinese that `verified` means identity/source verification passed but formal metadata still requires explicit `human_confirmed`.</action>
  <verify>python -m pytest tests/test_templates.py -q</verify>
  <acceptance_criteria>
    - `templates/verification_review.md` contains `# 候选文献核验记录`.
    - The review template contains all six exact field names from the action text.
    - The review template contains `字段说明`, `verified`, `rejected`, `uncertain`, and `human_confirmed`.
    - `src/rsa_cli/templates.py` built-in `verification_review.md` contains `candidate_title`.
  </acceptance_criteria>
  <done>The candidate review template matches the Phase 2 staging contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update search candidate template to point into review staging</name>
  <files>templates/search_candidates.md, src/rsa_cli/templates.py, tests/test_templates.py</files>
  <read_first>templates/search_candidates.md, src/rsa_cli/templates.py, .planning/phases/02-literature-records-pipeline/02-RESEARCH.md</read_first>
  <action>Update `templates/search_candidates.md` and `TEMPLATE_FILES[\"search_candidates.md\"]` to Chinese user-facing text. Keep it as candidate staging, not formal records. Include columns `candidate_title`, `source`, `doi_or_url`, `why_relevant`, and `next_review_action`. Add Chinese text saying candidates must be reviewed in `verification_review.md` and cannot create `metadata/P###.yaml` until `verified + human_confirmed` is explicitly provided through the formal metadata command.</action>
  <verify>python -m pytest tests/test_templates.py -q</verify>
  <acceptance_criteria>
    - `templates/search_candidates.md` contains `候选文献`.
    - `templates/search_candidates.md` contains `candidate_title`, `source`, `doi_or_url`, `why_relevant`, and `next_review_action`.
    - `templates/search_candidates.md` contains `verification_review.md` and `metadata/P###.yaml`.
    - Tests assert the template does not contain `C001.yaml`.
  </acceptance_criteria>
  <done>Candidate search output points to the review stage without weakening formal metadata boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add template and CLI regression coverage</name>
  <files>tests/test_templates.py, tests/test_cli.py</files>
  <read_first>tests/test_skeleton.py, tests/test_cli.py, templates/verification_review.md, templates/search_candidates.md</read_first>
  <action>Create `tests/test_templates.py` if it does not exist. Add tests that read root templates and `src/rsa_cli/templates.py` fallback strings to prove user-facing Chinese explanations exist, English field names stay exact, allowed review status values are documented, and no candidate template suggests automatic metadata writing. Add a CLI regression test proving `rsa init` writes the updated review/search templates into a temp project's `templates/` directory.</action>
  <verify>python -m pytest tests/test_templates.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `tests/test_templates.py` exists and imports or reads `TEMPLATE_FILES`.
    - A temp `rsa init` test proves generated `templates/verification_review.md` contains `candidate_title`.
    - A test proves generated templates include Chinese text and English field names.
    - `python -m pytest tests/test_templates.py tests/test_cli.py -q` exits 0.
  </acceptance_criteria>
  <done>Template behavior is locked by tests before index work depends on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_templates.py tests/test_cli.py -q`
- [ ] `Select-String -Path templates/verification_review.md -Pattern "candidate_title|verified|rejected|uncertain|human_confirmed"`
- [ ] `Select-String -Path templates/search_candidates.md -Pattern "metadata/P###.yaml|verification_review.md"`
- [ ] Confirm no `C001.yaml` or per-candidate YAML schema was introduced.
</verification>

<success_criteria>
- Candidate papers remain outside formal metadata until reviewed.
- Users can mark candidates `verified`, `rejected`, or `uncertain` with reasons and source evidence.
- Review/search templates are readable in Chinese while preserving stable English field names.
- Verified review rows do not automatically enter formal metadata or consume `P###`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-literature-records-pipeline/02-02-SUMMARY.md`
</output>
