---
phase: "02-literature-records-pipeline"
plan: "04"
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - src/rsa_cli/templates.py
  - templates/topic_profile.yaml
  - templates/round_readme.md
  - templates/final_round_summary.md
  - templates/paper_note.md
  - templates/map_integration.md
  - templates/pdf_acquisition_report.md
  - templates/reading_batch_report.md
  - 01_literature/literature_map.md
  - 01_literature/research_tables.md
  - 01_literature/agent_research_notes.md
  - tests/test_templates.py
  - tests/test_skeleton.py
autonomous: true
requirements: [META-01, META-02, SRCH-01, SRCH-02]
user_setup: []
must_haves:
  truths:
    - "D-01, D-02, D-03 are extended to all Phase 1 user-facing templates and formal-record seed files: English keys/placeholders stay stable, but user-visible explanations are Chinese."
    - "D-26 and D-27 are implemented by localizing the remaining Phase 1 English-only placeholders without changing the Phase 1 scaffold behavior."
    - "The plan does not implement Phase 3 mapping, Phase 4 reading-note generation, PDF acquisition automation, search adapters, or LLM orchestration."
    - "Existing template placeholders such as `{round_id}`, `{objective}`, `{topic_profile}`, `{max_candidates}`, `{allowed_tools}`, `{output_policy}`, `{approval_mode}`, and `{campaign_id}` remain intact."
  artifacts:
    - path: "templates/topic_profile.yaml"
      provides: "Topic profile template with Chinese field explanations"
      contains: "字段说明"
    - path: "templates/round_readme.md"
      provides: "Research round README template with Chinese labels"
      contains: "研究轮次"
    - path: "templates/final_round_summary.md"
      provides: "Round summary template with Chinese sections"
      contains: "人工确认"
    - path: "templates/pdf_acquisition_report.md"
      provides: "PDF status template with Chinese authorization warning"
      contains: "授权"
    - path: "tests/test_templates.py"
      provides: "Regression tests for remaining Phase 1 localization"
      min_lines: 80
  key_links:
    - from: "src/rsa_cli/templates.py"
      to: "templates/round_readme.md"
      via: "built-in fallback mirrors root template wording and placeholders"
      pattern: "{round_id}"
    - from: "src/rsa_cli/templates.py"
      to: "templates/topic_profile.yaml"
      via: "built-in fallback keeps English YAML keys with Chinese explanations"
      pattern: "topic_id"
---

<objective>
Localize remaining Phase 1 user-facing templates and formal-record seed files.

Purpose: Remove English-only placeholders left by the Phase 1 scaffold so later phases start from Chinese-readable templates, while keeping English YAML keys, CLI-related placeholders, and existing harness behavior stable.
Output: Chinese-explained topic/round/note/map/PDF/reading templates, Chinese formal-record seed guidance, and tests proving placeholders and English keys remain intact.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-literature-records-pipeline/02-CONTEXT.md
@.planning/phases/02-literature-records-pipeline/02-RESEARCH.md
@.planning/phases/02-literature-records-pipeline/02-VALIDATION.md
@.planning/phases/02-literature-records-pipeline/02-PATTERNS.md
@.planning/phases/02-literature-records-pipeline/02-02-PLAN.md
@.planning/phases/02-literature-records-pipeline/02-03-PLAN.md
@src/rsa_cli/templates.py
@templates/topic_profile.yaml
@templates/round_readme.md
@templates/final_round_summary.md
@templates/paper_note.md
@templates/map_integration.md
@templates/pdf_acquisition_report.md
@templates/reading_batch_report.md
@01_literature/literature_map.md
@01_literature/research_tables.md
@01_literature/agent_research_notes.md
@AGENTS.md
</context>

<threat_model>
- T-02-10 Placeholder breakage: changing template text can accidentally remove `{...}` format placeholders used by round creation. Mitigate with tests for every required placeholder in `round_readme.md` and summary templates.
- T-02-11 Scope creep through localization: adding Chinese text can accidentally imply implemented map, PDF download, or reading-note behavior. Mitigate by wording templates as placeholders/status records only and testing for explicit "not automatic" guidance.
- T-02-12 English key drift: translating YAML keys can break validators and scripts. Mitigate by preserving English YAML keys and adding Chinese explanations alongside them.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Localize topic and round templates</name>
  <files>templates/topic_profile.yaml, templates/round_readme.md, templates/final_round_summary.md, src/rsa_cli/templates.py, tests/test_templates.py</files>
  <read_first>templates/topic_profile.yaml, templates/round_readme.md, templates/final_round_summary.md, src/rsa_cli/templates.py, .planning/phases/02-literature-records-pipeline/02-CONTEXT.md</read_first>
  <action>Update `templates/topic_profile.yaml` and `TEMPLATE_FILES[\"topic_profile.yaml\"]` so English YAML keys remain exactly `topic_id`, `topic_name`, `core_keywords`, `priority_questions`, `important_metrics`, `preferred_sources`, `exclude_scope`, `grade_rules`, and `required_outputs`, while Chinese comments or a `字段说明` block explain each key. Update `templates/round_readme.md` and `TEMPLATE_FILES[\"round_readme.md\"]` to Chinese user-facing headings while preserving `{round_id}`, `{objective}`, `{topic_profile}`, `{max_candidates}`, `{allowed_tools}`, `{output_policy}`, `{approval_mode}`, and `{campaign_id}`. Update `templates/final_round_summary.md` and its fallback to Chinese section names including `状态`, `关键发现`, `候选决策`, `请求写入正式记录`, and `人工确认`.</action>
  <verify>python -m pytest tests/test_templates.py tests/test_skeleton.py -q</verify>
  <acceptance_criteria>
    - `templates/topic_profile.yaml` still contains every required English YAML key.
    - `templates/topic_profile.yaml` contains `字段说明`.
    - `templates/round_readme.md` contains all eight `{...}` placeholders listed in the action.
    - `templates/final_round_summary.md` contains `人工确认`.
    - `python -m pytest tests/test_templates.py tests/test_skeleton.py -q` exits 0.
  </acceptance_criteria>
  <done>Topic and round templates are Chinese-readable without breaking existing round creation.</done>
</task>

<task type="auto">
  <name>Task 2: Localize note, map, PDF, and reading report seeds without adding behavior</name>
  <files>templates/paper_note.md, templates/map_integration.md, templates/pdf_acquisition_report.md, templates/reading_batch_report.md, src/rsa_cli/templates.py, tests/test_templates.py</files>
  <read_first>templates/paper_note.md, templates/map_integration.md, templates/pdf_acquisition_report.md, templates/reading_batch_report.md, src/rsa_cli/templates.py, .planning/phases/02-literature-records-pipeline/02-CONTEXT.md</read_first>
  <action>Update `templates/paper_note.md`, `templates/map_integration.md`, `templates/pdf_acquisition_report.md`, and `templates/reading_batch_report.md`, plus the matching `TEMPLATE_FILES` entries, to Chinese user-facing headings and guidance. Keep them as templates/status reports only. `paper_note.md` must state that full-text reading requires local/provided/authorized content. `map_integration.md` must state that formal map updates belong to a later approved workflow. `pdf_acquisition_report.md` must state that no unauthorized PDF downloading is allowed and only status/source/authorization should be recorded. `reading_batch_report.md` must state that reading outputs require human approval before formal use.</action>
  <verify>python -m pytest tests/test_templates.py -q</verify>
  <acceptance_criteria>
    - `templates/paper_note.md` contains `授权` and `人工决策`.
    - `templates/map_integration.md` contains `正式映射`.
    - `templates/pdf_acquisition_report.md` contains `不得下载未授权 PDF`.
    - `templates/reading_batch_report.md` contains `人工确认`.
    - Matching strings exist in `src/rsa_cli/templates.py` fallback templates.
  </acceptance_criteria>
  <done>Later-phase template seeds are readable and safe without pretending those later workflows exist yet.</done>
</task>

<task type="auto">
  <name>Task 3: Localize formal record seed files and regression tests</name>
  <files>01_literature/literature_map.md, 01_literature/research_tables.md, 01_literature/agent_research_notes.md, src/rsa_cli/templates.py, tests/test_templates.py, tests/test_skeleton.py</files>
  <read_first>01_literature/literature_map.md, 01_literature/research_tables.md, 01_literature/agent_research_notes.md, src/rsa_cli/templates.py, tests/test_skeleton.py</read_first>
  <action>Update `01_literature/literature_map.md`, `01_literature/research_tables.md`, and `01_literature/agent_research_notes.md` to Chinese guidance text. Mirror the same Chinese seed text in `FORMAL_RECORD_FILES` in `src/rsa_cli/templates.py` so future `rsa init` projects get localized seeds. Add tests proving `rsa init` creates localized formal record seed files in a temp project and still does not overwrite human-edited files on a second init.</action>
  <verify>python -m pytest tests/test_templates.py tests/test_skeleton.py -q</verify>
  <acceptance_criteria>
    - `01_literature/literature_map.md` contains `文献映射`.
    - `01_literature/research_tables.md` contains `研究表格`.
    - `01_literature/agent_research_notes.md` contains `辅助材料`.
    - A temp `rsa init` test proves generated formal record seed files contain Chinese guidance.
    - Existing idempotence test still proves a human-edited file is not overwritten.
  </acceptance_criteria>
  <done>Formal record seeds are Chinese-readable and preserve the idempotent scaffold contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_templates.py tests/test_skeleton.py -q`
- [ ] `python -m pytest -q`
- [ ] Confirm no required `{...}` placeholders were removed from `round_readme.md`.
- [ ] Confirm no template says PDF download, map update, or reading-note generation happens automatically.
</verification>

<success_criteria>
- Remaining Phase 1 user-facing templates have Chinese explanations.
- English YAML keys and template placeholders remain stable.
- Formal record seed files are Chinese-readable.
- No later-phase behavior is implemented or implied by this localization cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-literature-records-pipeline/02-04-SUMMARY.md`
</output>
