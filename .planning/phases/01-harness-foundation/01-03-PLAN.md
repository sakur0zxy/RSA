---
phase: "01-harness-foundation"
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/rsa_cli/rounds.py
  - src/rsa_cli/cli.py
  - src/rsa_cli/config.py
  - templates/round_readme.md
  - templates/final_round_summary.md
  - templates/search_candidates.md
  - templates/verification_review.md
  - templates/map_integration.md
  - templates/pdf_acquisition_report.md
  - templates/reading_batch_report.md
  - tests/test_rounds.py
  - tests/test_cli.py
  - AGENTS.md
autonomous: true
requirements: [ROUND-01, DOCS-01, PROF-01, PROF-02]
user_setup: []
must_haves:
  truths:
    - "ROUND-01 and D-10, D-11, D-12, D-13, D-14, D-15 are implemented by `rsa new-round` with bounded candidate limits, reserved campaign metadata, and required round files."
    - "D-18, D-19, and D-20 are preserved because round archives reference local assets and formal-write policy but do not place PDFs, screenshots, or unconfirmed facts into formal records."
    - "D-21, D-22, D-23, D-24, D-25, D-26, and D-27 are implemented by a minimal runnable `rsa` CLI with init, validate-profile, and new-round commands, while excluding real search, PDF acquisition, metadata verification, mapping, reading, and LLM calls."
    - "DOCS-01 is satisfied because AGENTS.md documents evidence hierarchy, CLI entry points, local asset policy, and human confirmation rules for future agents."
  artifacts:
    - path: "src/rsa_cli/rounds.py"
      provides: "Bounded research round directory creation"
      contains: "create_research_round"
    - path: "src/rsa_cli/cli.py"
      provides: "new-round subcommand"
      contains: "new-round"
    - path: "tests/test_rounds.py"
      provides: "Round naming, cap, and required-file tests"
      min_lines: 50
    - path: "tests/test_cli.py"
      provides: "CLI smoke tests for init, validate-profile, and new-round"
      min_lines: 30
    - path: "AGENTS.md"
      provides: "Project instructions for future agents"
      contains: "rsa new-round"
  key_links:
    - from: "src/rsa_cli/cli.py"
      to: "src/rsa_cli/rounds.py"
      via: "new-round command calls round creator"
      pattern: "create_research_round"
    - from: "src/rsa_cli/rounds.py"
      to: "src/rsa_cli/profiles.py"
      via: "round creation validates the selected topic profile first"
      pattern: "validate_topic_profile"
    - from: "src/rsa_cli/rounds.py"
      to: "templates/round_readme.md"
      via: "round README is rendered from the template"
      pattern: "round_readme"
---

<objective>
Implement research round initialization and finalize Phase 1 guidance.

Purpose: Make the harness usable end to end for the first bounded SAR research round without crossing into real search, PDF acquisition, metadata verification, mapping, reading, or LLM orchestration.
Output: `rsa new-round`, required round archive files, CLI smoke tests, and updated project instructions for future agents.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-harness-foundation/01-CONTEXT.md
@.planning/phases/01-harness-foundation/01-RESEARCH.md
@.planning/phases/01-harness-foundation/01-VALIDATION.md
@.planning/phases/01-harness-foundation/01-01-SUMMARY.md
@.planning/phases/01-harness-foundation/01-02-SUMMARY.md
@src/rsa_cli/cli.py
@src/rsa_cli/config.py
@src/rsa_cli/profiles.py
@templates/round_readme.md
@templates/final_round_summary.md
@AGENTS.md
</context>

<threat_model>
- T-03-01 Users can bypass the candidate cap if CLI overrides are not checked against the hard limit. Mitigate by enforcing hard cap in `rounds.py` and testing cap overflow.
- T-03-02 Round creation can pollute formal records if it writes metadata, maps, or notes. Mitigate by writing only under `agent_outputs/R###_slug/` in Phase 1.
- T-03-03 Unsafe round names can escape the configured output directory. Mitigate by deriving slugs from allowed characters and rejecting path separators.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded round creation service</name>
  <files>src/rsa_cli/rounds.py, src/rsa_cli/config.py, templates/round_readme.md, templates/final_round_summary.md, templates/search_candidates.md, templates/verification_review.md, templates/map_integration.md, templates/pdf_acquisition_report.md, templates/reading_batch_report.md, tests/test_rounds.py</files>
  <read_first>.planning/phases/01-harness-foundation/01-CONTEXT.md, src/rsa_cli/config.py, src/rsa_cli/profiles.py</read_first>
  <action>Create `create_research_round(config, topic_profile_path, objective, name, max_candidates=None, allowed_tools=None, output_policy=None, approval_mode=None, campaign_id=None)`. The function must validate the topic profile first, enforce default max 5 and hard cap 20, derive `R001_short_topic_name` style folders under the configured `agent_outputs` path, increment to the next available round number, reject path separators in user-provided names, and write required `README.md` and `final_round_summary.md`. Include campaign_id in the round README when provided, but do not create nested campaign directories. Optional round templates should exist in `templates/` but should not be required files for a new round.</action>
  <verify>python -m pytest tests/test_rounds.py -q</verify>
  <acceptance_criteria>
    - Creating the first round produces `01_literature/agent_outputs/R001_short_topic_name/README.md`.
    - `final_round_summary.md` is created in the same round directory.
    - `--max-candidates 21` or equivalent service input fails before any round directory is created.
    - A name containing `/` or `..` fails before any round directory is created.
    - No files are written under `metadata`, `paper_index.md`, `literature_map.md`, or `notes`.
  </acceptance_criteria>
  <done>Research rounds can be staged safely and traceably without formal-record writes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new-round CLI and smoke tests</name>
  <files>src/rsa_cli/cli.py, tests/test_cli.py, tests/test_rounds.py</files>
  <read_first>src/rsa_cli/cli.py, src/rsa_cli/rounds.py, .planning/phases/01-harness-foundation/01-VALIDATION.md</read_first>
  <action>Add `rsa new-round` with arguments `--topic PATH`, `--objective TEXT`, `--name TEXT`, optional `--max-candidates N`, optional repeated `--allowed-tool VALUE`, optional `--output-policy TEXT`, optional `--approval-mode TEXT`, and optional `--campaign-id VALUE`. The command should print the created round path and exit 0 on success. It should exit nonzero with a concise actionable message when profile validation fails, candidate limits are invalid, or the round name is unsafe. Add CLI tests for `rsa --help`, `rsa init`, `rsa validate-profile`, and `rsa new-round` using temp directories.</action>
  <verify>python -m pytest tests/test_cli.py tests/test_rounds.py -q</verify>
  <acceptance_criteria>
    - `rsa --help` lists `init`, `validate-profile`, and `new-round`.
    - `rsa new-round` validates the topic profile before writing a round.
    - CLI failures do not show raw tracebacks for expected user errors.
    - CLI tests run without requiring network access or external APIs.
  </acceptance_criteria>
  <done>The Phase 1 CLI can perform the intended end-to-end local workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize project guidance and run SAR starter smoke path</name>
  <files>AGENTS.md, tests/test_cli.py</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/phases/01-harness-foundation/01-CONTEXT.md</read_first>
  <action>Update AGENTS.md so future agents know the Phase 1 CLI entry points, the evidence hierarchy, local-only PDF and screenshot policy, and the rule that formal records require human confirmation. Then run the smoke path: install editable package, run `rsa init`, validate `01_literature/topic_profiles/sar_noncontinuous_aperture.yaml`, and create one SAR starter round in `agent_outputs` with max candidates 5. Do not perform real search, PDF acquisition, metadata verification, mapping, reading, or LLM calls.</action>
  <verify>python -m pytest tests/test_cli.py tests/test_config.py tests/test_profiles.py tests/test_skeleton.py tests/test_rounds.py -q</verify>
  <acceptance_criteria>
    - AGENTS.md mentions `rsa init`, `rsa validate-profile`, and `rsa new-round`.
    - AGENTS.md states that PDFs and screenshots/assets are local-only by default.
    - AGENTS.md states that formal metadata, maps, notes, and research records need human confirmation before writes.
    - The SAR starter smoke path succeeds locally and only creates round staging files under `agent_outputs`.
  </acceptance_criteria>
  <done>Future agents have the guidance and smoke-tested CLI path needed to begin Phase 2 safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_cli.py tests/test_config.py tests/test_profiles.py tests/test_skeleton.py tests/test_rounds.py -q`
- [ ] `python -m pip install -e ".[dev]"`
- [ ] `rsa init`
- [ ] `rsa validate-profile 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml`
- [ ] `rsa new-round --topic 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml --objective "Smoke test SAR starter profile" --name "sar starter smoke" --max-candidates 5`
- [ ] Confirm the smoke round contains `README.md` and `final_round_summary.md` and does not modify formal metadata or maps.
</verification>

<success_criteria>
- `rsa init`, `rsa validate-profile`, and `rsa new-round` all work in the local project.
- Research round folders are bounded, traceable, and staged under `agent_outputs`.
- Round limits, output policy, approval mode, and optional campaign id are captured in the round archive.
- AGENTS.md gives future agents enough project-specific guidance to respect the evidence hierarchy and local-only asset policy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-foundation/01-03-SUMMARY.md`
</output>
