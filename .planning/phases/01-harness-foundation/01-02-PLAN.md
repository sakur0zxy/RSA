---
phase: "01-harness-foundation"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/rsa_cli/config.py
  - src/rsa_cli/profiles.py
  - src/rsa_cli/cli.py
  - 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml
  - tests/test_config.py
  - tests/test_profiles.py
autonomous: true
requirements: [PROF-01, PROF-02, ROUND-01]
user_setup: []
must_haves:
  truths:
    - "PROF-01 and PROF-02 are implemented by schema validation that checks D-06, D-07, D-08, and D-09 without making SAR a code path."
    - "ROUND-01 and D-10, D-11, D-12 are represented in settings by default candidate limits, a hard cap, allowed tools, output policy, approval mode, and reserved campaign_id support."
    - "D-20, D-22, and D-23 are preserved because validation reads configured YAML safely and reports actionable errors before later formal writes can occur."
  artifacts:
    - path: "src/rsa_cli/profiles.py"
      provides: "Topic profile schema validation"
      contains: "REQUIRED_PROFILE_FIELDS"
    - path: "src/rsa_cli/cli.py"
      provides: "validate-profile subcommand"
      contains: "validate-profile"
    - path: "tests/test_profiles.py"
      provides: "Passing and failing profile validation tests"
      min_lines: 40
    - path: "src/rsa_cli/config.py"
      provides: "Round defaults and hard cap configuration"
      contains: "hard_max_candidates"
  key_links:
    - from: "src/rsa_cli/cli.py"
      to: "src/rsa_cli/profiles.py"
      via: "validate-profile command calls profile validator"
      pattern: "validate_topic_profile"
    - from: "src/rsa_cli/profiles.py"
      to: "PyYAML"
      via: "profile YAML parsed with safe_load"
      pattern: "safe_load"
---

<objective>
Implement topic profile validation and round configuration contracts.

Purpose: Turn the Phase 1 profile schema and bounded-round defaults into executable checks so later phases can trust profile files and candidate limits.
Output: `rsa validate-profile`, explicit profile schema validation, round-related config defaults, and tests for complete and invalid profiles.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-harness-foundation/01-CONTEXT.md
@.planning/phases/01-harness-foundation/01-RESEARCH.md
@.planning/phases/01-harness-foundation/01-VALIDATION.md
@.planning/phases/01-harness-foundation/01-01-SUMMARY.md
@src/rsa_cli/config.py
@src/rsa_cli/cli.py
@01_literature/topic_profiles/sar_noncontinuous_aperture.yaml
</context>

<threat_model>
- T-02-01 Unsafe YAML parsing can execute unintended constructors. Mitigate by using only `yaml.safe_load` for profiles and config.
- T-02-02 Loose validation can let broken profiles into later research rounds. Mitigate by returning every missing or invalid field in one error list.
- T-02-03 Candidate limits can drift if command code and config defaults disagree. Mitigate by keeping default and hard cap values in one settings object with tests.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Harden config model for bounded rounds</name>
  <files>src/rsa_cli/config.py, tests/test_config.py</files>
  <read_first>src/rsa_cli/config.py, rsa.yaml, .planning/phases/01-harness-foundation/01-CONTEXT.md</read_first>
  <action>Extend the config loader with explicit accessors or dataclasses for `literature_root`, `templates_root`, `profiles.root`, and `rounds`. The `rounds` config must expose `default_max_candidates`, `hard_max_candidates`, `allowed_tools`, `output_policy`, `approval_mode`, and optional `campaign_id`. Validate that default max is positive and not above hard max. Keep local overrides recursive and deterministic.</action>
  <verify>python -m pytest tests/test_config.py -q</verify>
  <acceptance_criteria>
    - Tests prove `rounds.default_max_candidates` defaults to 5.
    - Tests prove `rounds.hard_max_candidates` defaults to 20.
    - Tests prove invalid default greater than hard cap raises an actionable config error.
    - Tests prove local override can change the literature root without changing source code.
  </acceptance_criteria>
  <done>Round defaults are centralized and enforce the Phase 1 bounded-round contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement topic profile validator and CLI command</name>
  <files>src/rsa_cli/profiles.py, src/rsa_cli/cli.py, tests/test_profiles.py</files>
  <read_first>templates/topic_profile.yaml, 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml, .planning/phases/01-harness-foundation/01-VALIDATION.md</read_first>
  <action>Create `profiles.py` with `REQUIRED_PROFILE_FIELDS` and `validate_topic_profile(path: Path) -> list[str]` or an equivalent result object. Validate that required fields exist, string fields are non-empty, list fields are lists with at least one item, `grade_rules` defines candidate-priority rules, and `required_outputs` is non-empty. Wire `rsa validate-profile PATH` so a valid profile prints a concise pass message and exits 0, while invalid YAML or schema errors print all errors and exit nonzero.</action>
  <verify>python -m pytest tests/test_profiles.py -q</verify>
  <acceptance_criteria>
    - A complete temp profile passes validation.
    - A profile missing several required fields reports all missing field names in one run.
    - Invalid YAML produces an actionable error without a traceback in normal CLI output.
    - `rsa validate-profile 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml` exits 0.
  </acceptance_criteria>
  <done>Users can validate topic profile YAML and get actionable schema errors.</done>
</task>

<task type="auto">
  <name>Task 3: Verify starter SAR profile remains generic data</name>
  <files>01_literature/topic_profiles/sar_noncontinuous_aperture.yaml, tests/test_profiles.py</files>
  <read_first>.planning/phases/01-harness-foundation/01-CONTEXT.md, .planning/phases/01-harness-foundation/01-RESEARCH.md</read_first>
  <action>Adjust the SAR starter profile only if validation exposes missing fields or invalid shapes. Add tests that assert the starter profile passes the generic validator and contains the key research coverage terms from D-09. Do not add any SAR-specific conditional logic to `profiles.py` or `cli.py`; the profile must be data that any topic profile can emulate.</action>
  <verify>python -m pytest tests/test_profiles.py -q</verify>
  <acceptance_criteria>
    - The starter profile passes the same validator used for any topic.
    - Tests check for noncontinuous aperture, distributed SAR, cross-channel coherence, phase-history recovery, ghost metrics, failure boundaries, and physics-constrained learning.
    - No Python validator branch checks for `sar`, `noncontinuous`, or the starter topic id.
  </acceptance_criteria>
  <done>The starter profile is both realistic and proof that the harness is topic-generic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_config.py tests/test_profiles.py -q`
- [ ] `rsa validate-profile 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml`
- [ ] Create a temp invalid profile missing at least three required fields and confirm all missing fields are reported.
- [ ] Search Python source to confirm `yaml.load` is absent and `yaml.safe_load` is present.
</verification>

<success_criteria>
- Profile validation covers every required PROF-01 field.
- Invalid profiles fail with actionable, aggregated errors.
- Round limits and approval policy are represented in runtime settings.
- SAR remains a starter profile instance, not a special implementation branch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-foundation/01-02-SUMMARY.md`
</output>
