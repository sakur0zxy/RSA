---
phase: "01-harness-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - rsa.yaml
  - src/rsa_cli/__init__.py
  - src/rsa_cli/cli.py
  - src/rsa_cli/config.py
  - src/rsa_cli/skeleton.py
  - src/rsa_cli/templates.py
  - templates/topic_profile.yaml
  - templates/paper_metadata.yaml
  - templates/paper_note.md
  - templates/round_readme.md
  - templates/final_round_summary.md
  - templates/search_candidates.md
  - templates/verification_review.md
  - templates/map_integration.md
  - templates/pdf_acquisition_report.md
  - templates/reading_batch_report.md
  - 01_literature/metadata/.gitkeep
  - 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml
  - 01_literature/agent_outputs/.gitkeep
  - 01_literature/notes/.gitkeep
  - 01_literature/synthesis/.gitkeep
  - 01_literature/pdfs/.gitkeep
  - 01_literature/assets/.gitkeep
  - 01_literature/paper_index.md
  - 01_literature/literature_map.md
  - 01_literature/research_tables.md
  - 01_literature/agent_research_notes.md
  - tests/test_config.py
  - tests/test_skeleton.py
autonomous: true
requirements: [DOCS-01, PROF-01]
user_setup: []
must_haves:
  truths:
    - "DOCS-01 and D-01, D-02, D-03, D-04, D-05 are implemented by a configured literature root, committed project defaults, optional local overrides, the full literature foundation, and root templates."
    - "PROF-01 and D-06, D-07, D-08, D-09 are represented by a generic topic profile template plus a detailed SAR starter profile, with no SAR-specific branching in CLI code."
    - "D-16, D-17, D-18, D-19 are protected by default paths and gitignore rules for local PDFs and important screenshots/assets."
    - "D-20 is preserved because scaffold creation is idempotent and never overwrites edited formal records."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python package metadata with rsa console script and PyYAML dependency"
      contains: "rsa ="
    - path: "rsa.yaml"
      provides: "Committed project defaults for literature root, templates root, and round limits"
      contains: "literature_root"
    - path: "01_literature/topic_profiles/sar_noncontinuous_aperture.yaml"
      provides: "Detailed SAR starter topic profile data"
      contains: "noncontinuous"
    - path: ".gitignore"
      provides: "Local-only protection for PDF, asset, and local settings files"
      contains: ".rsa/local.yaml"
  key_links:
    - from: "src/rsa_cli/cli.py"
      to: "src/rsa_cli/skeleton.py"
      via: "rsa init command calls the skeleton creator"
      pattern: "create_literature_skeleton"
    - from: "src/rsa_cli/config.py"
      to: "rsa.yaml"
      via: "project defaults are loaded before local overrides"
      pattern: "rsa.yaml"
---

<objective>
Create the local project scaffold for Phase 1.

Purpose: Establish the file layout, default settings, root templates, and starter SAR topic profile that later validation and round commands will use.
Output: A runnable Python package shell with `rsa init`, committed `rsa.yaml`, protected local-only paths, the full `01_literature/` foundation, root templates, and focused tests for config and skeleton behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-harness-foundation/01-CONTEXT.md
@.planning/phases/01-harness-foundation/01-RESEARCH.md
@.planning/phases/01-harness-foundation/01-VALIDATION.md
@AGENTS.md
</context>

<threat_model>
- T-01-01 Local-only assets can leak into git if `.gitignore` misses `.rsa/local.yaml`, `01_literature/pdfs/`, or `01_literature/assets/`. Mitigate with explicit ignore rules and tests.
- T-01-02 `rsa init` can destroy formal records if it overwrites existing files. Mitigate with write-if-missing behavior and idempotence tests.
- T-01-03 Settings can become ambiguous if local overrides and project defaults are merged in the wrong order. Mitigate with explicit tests where `.rsa/local.yaml` wins.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create package scaffold and config loader</name>
  <files>pyproject.toml, src/rsa_cli/__init__.py, src/rsa_cli/cli.py, src/rsa_cli/config.py, tests/test_config.py</files>
  <read_first>.planning/phases/01-harness-foundation/01-RESEARCH.md, .planning/phases/01-harness-foundation/01-CONTEXT.md</read_first>
  <action>Create a Python package named `rsa_cli` with console script `rsa = rsa_cli.cli:main`. Add dependencies `PyYAML>=6.0.3` and dev dependency `pytest`. Implement a small argparse router with an `init` subcommand. Implement `load_project_config(root: Path)` in `config.py` using `yaml.safe_load`, reading `rsa.yaml` first and `.rsa/local.yaml` second, recursively merging dictionaries so local values win. Missing `.rsa/local.yaml` must be accepted. Resolve configured relative paths against the project root, not the process working directory.</action>
  <verify>python -m pytest tests/test_config.py -q</verify>
  <acceptance_criteria>
    - `pyproject.toml` declares the `rsa` console script and PyYAML dependency.
    - `load_project_config` uses `yaml.safe_load`, not `yaml.load`.
    - A temp-project test proves local overrides beat `rsa.yaml`.
    - A temp-project test proves missing `.rsa/local.yaml` does not fail.
  </acceptance_criteria>
  <done>Config loading and the package entry point are ready for later commands.</done>
</task>

<task type="auto">
  <name>Task 2: Create idempotent literature skeleton and template files</name>
  <files>.gitignore, rsa.yaml, src/rsa_cli/skeleton.py, src/rsa_cli/templates.py, templates/topic_profile.yaml, templates/paper_metadata.yaml, templates/paper_note.md, templates/round_readme.md, templates/final_round_summary.md, templates/search_candidates.md, templates/verification_review.md, templates/map_integration.md, templates/pdf_acquisition_report.md, templates/reading_batch_report.md, 01_literature/metadata/.gitkeep, 01_literature/agent_outputs/.gitkeep, 01_literature/notes/.gitkeep, 01_literature/synthesis/.gitkeep, 01_literature/pdfs/.gitkeep, 01_literature/assets/.gitkeep, 01_literature/paper_index.md, 01_literature/literature_map.md, 01_literature/research_tables.md, 01_literature/agent_research_notes.md, tests/test_skeleton.py</files>
  <read_first>.planning/phases/01-harness-foundation/01-CONTEXT.md, .planning/phases/01-harness-foundation/01-VALIDATION.md</read_first>
  <action>Implement `create_literature_skeleton(config)` so `rsa init` creates the configured foundation without overwriting existing user-edited files. Default `rsa.yaml` must set `literature_root: "01_literature"`, `templates_root: "templates"`, `rounds.default_max_candidates: 5`, `rounds.hard_max_candidates: 20`, and a human-confirmation oriented approval mode. Create root templates for topic profiles, paper metadata, paper notes, required round files, and optional round files. Add `.gitignore` rules for `.rsa/local.yaml`, `01_literature/pdfs/*`, and `01_literature/assets/*`, while allowing `.gitkeep` files to keep the directory structure visible.</action>
  <verify>python -m pytest tests/test_skeleton.py -q</verify>
  <acceptance_criteria>
    - `rsa init` creates `metadata`, `topic_profiles`, `agent_outputs`, `notes`, `synthesis`, `pdfs`, `assets`, `paper_index.md`, `literature_map.md`, `research_tables.md`, and `agent_research_notes.md`.
    - Existing Markdown, YAML, and template files are not overwritten on a second `rsa init`.
    - `.gitignore` contains local-only rules for `.rsa/local.yaml`, PDF files, and asset files.
    - Root `templates/` contains both required and optional round templates.
  </acceptance_criteria>
  <done>The project has the full file foundation required by Phase 1 and protected local asset paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add generic topic template and SAR starter profile</name>
  <files>templates/topic_profile.yaml, 01_literature/topic_profiles/sar_noncontinuous_aperture.yaml, tests/test_skeleton.py</files>
  <read_first>.planning/phases/01-harness-foundation/01-CONTEXT.md, .planning/phases/01-harness-foundation/01-RESEARCH.md</read_first>
  <action>Create `templates/topic_profile.yaml` with all required generic fields: `topic_id`, `topic_name`, `core_keywords`, `priority_questions`, `important_metrics`, `preferred_sources`, `exclude_scope`, `grade_rules`, and `required_outputs`. Create `01_literature/topic_profiles/sar_noncontinuous_aperture.yaml` as a real starter instance, not a placeholder. It must cover noncontinuous aperture, wide-angle or wide-aperture SAR, multi-aspect or subaperture SAR, distributed SAR, cross-channel coherence, phase-history recovery, ghost metrics, failure boundaries, and physics-constrained learning. Keep these details as data in YAML; do not hard-code SAR into Python modules.</action>
  <verify>python -m pytest tests/test_skeleton.py -q</verify>
  <acceptance_criteria>
    - The topic template lists every required field named in PROF-01.
    - The SAR starter profile includes the detailed research topics named in D-09.
    - Python source contains no SAR-only branches or profile-specific constants.
  </acceptance_criteria>
  <done>Phase 1 has a realistic SAR starter profile while the harness remains generic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_config.py tests/test_skeleton.py -q`
- [ ] `python -m pip install -e ".[dev]"`
- [ ] `rsa --help` exits successfully.
- [ ] `rsa init` can run twice without changing existing scaffold files.
- [ ] `git status --ignored --short` shows generated PDF and asset payloads are ignored, while `.gitkeep` files remain trackable.
</verification>

<success_criteria>
- All scaffold files, templates, and default settings exist.
- `rsa init` creates the configured literature foundation idempotently.
- The SAR starter topic profile is detailed enough for later smoke tests.
- Local PDF, screenshot, and local settings paths are protected from accidental commits.
</success_criteria>

<output>
After completion, create `.planning/phases/01-harness-foundation/01-01-SUMMARY.md`
</output>
