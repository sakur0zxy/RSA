---
phase: "06-asset-source-foundation"
plan: "06-02"
type: "feature"
wave: 2
depends_on:
  - "06-01"
files_modified:
  - "src/rsa_cli/cli.py"
  - "README.md"
  - ".planning/PROJECT.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - "tests/test_cli.py"
  - "tests/test_templates.py"
autonomous: true
requirements:
  - "V2-ASSET-04"
  - "V2-SRC-01"
  - "V2-ASSET-01"
---

# Plan 06-02: CLI Integration And Documentation

<objective>
Expose Phase 6 source and asset foundation through the CLI, update user-facing docs, and verify the whole harness remains stable.
</objective>

<tasks>

1. Add `rsa source add|validate|status` to `cli.py`.
2. Add `rsa asset add|validate|status` to `cli.py`.
3. Ensure all user-facing output is Chinese-first and errors have no traceback for expected failures.
4. Update README and GSD docs to mention Phase 6 completion and new commands.
5. Add CLI tests covering help surface and at least one source/asset happy path.
6. Run targeted tests and full test suite.

</tasks>

<verification>

- `python -m pytest tests/test_assets.py tests/test_cli.py tests/test_templates.py -q`
- `python -m pytest -q`
- `python -m rsa_cli.cli --root . eval compare`
- `git diff --check`

</verification>

<success_criteria>

- Users can register sources and assets via CLI.
- Read-only validate/status commands do not mutate files.
- Full tests pass.
- Phase 6 has summaries and verification artifacts.
</success_criteria>
