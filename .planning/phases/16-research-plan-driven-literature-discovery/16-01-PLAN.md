---
phase: "16"
plan: "01"
title: Discovery Profile And Query Bundle Foundation
status: completed
wave: 1
type: implementation
depends_on: []
autonomous: true
files_modified:
  - src/rsa_cli/config.py
  - src/rsa_cli/discovery.py
  - src/rsa_cli/skeleton.py
  - tests/test_discovery.py
  - tests/test_skeleton.py
requirements_addressed:
  - V2-DISCOVERY-01
  - V2-DISCOVERY-02
  - V2-DISCOVERY-03
  - V2-DISCOVERY-07
---

# Phase 16-01 Plan: Discovery Profile And Query Bundle Foundation

## Objective

<objective>
Add the local discovery artifact foundation for Phase 16: config paths, discovery ids, plan source parsing, query bundle generation, status semantics and validation. This plan creates auditable staging records only and does not search providers or create campaign items yet.
</objective>

## Read First

<read_first>
- `.planning/phases/16-research-plan-driven-literature-discovery/16-CONTEXT.md`
- `.planning/REQUIREMENTS.md`
- `src/rsa_cli/config.py`
- `src/rsa_cli/templates.py`
- `tests/test_skeleton.py`
</read_first>

## Must Haves

<must_haves>
<truth id="D-01">D-01: Phase 16 is plan-to-campaign discovery and must not write formal records.</truth>
<truth id="D-02">D-02: v1 accepts Markdown/text plan files, plain objectives and optional topic profiles; docx/Zotero/BibTeX remain future interfaces.</truth>
<truth id="D-03">D-03: Discovery records must preserve stable English keys with Chinese explanations and status/recovery text.</truth>
<truth id="D-06">D-06: Unreadable plans, empty queries or unsupported inputs fail closed with Chinese repair hints.</truth>
<truth id="D-07">D-07: User-visible discovery output is Chinese-first.</truth>
</must_haves>

## Tasks

<tasks>
<task id="16-01-T1" type="auto">
<files>
- `src/rsa_cli/config.py`
- `src/rsa_cli/skeleton.py`
- `tests/test_config.py`
- `tests/test_skeleton.py`
</files>
<action>
Add discovery configuration and filesystem roots:
- `DEFAULT_CONFIG["discovery"]` with conservative defaults;
- `ProjectConfig.discovery` and `ProjectConfig.discovery_root`;
- initialized `01_literature/discovery/` directory in skeleton setup if needed.
</action>
<acceptance_criteria>
- `discovery_root` resolves under `01_literature/discovery`.
- Existing config validation still passes.
- Skeleton initialization creates the discovery directory.
</acceptance_criteria>
</task>

<task id="16-01-T2" type="auto">
<files>
- `src/rsa_cli/discovery.py`
- `tests/test_discovery.py`
</files>
<action>
Implement discovery ids, artifact paths, plan source loading and query bundle generation:
- support `.md` and `.txt` files plus direct objective text;
- record `plan_source`, `research_questions`, `keywords`, `query_bundle`, `status`, `reason_zh`, `repair_hint_zh`;
- generate deterministic query candidates with `query_id`, `query_text`, `query_terms`, `rationale_zh`, `confidence`, `evidence_level`.
</action>
<acceptance_criteria>
- Valid plan input creates `DR###_profile.yaml` and `DR###_queries.yaml`.
- Empty or unreadable plan input raises Chinese-first `DiscoveryError`.
- Query records preserve English keys and Chinese explanations.
</acceptance_criteria>
</task>

<task id="16-01-T3" type="auto">
<files>
- `src/rsa_cli/discovery.py`
- `tests/test_discovery.py`
</files>
<action>
Implement read-only validation and status helpers for discovery profile/query artifacts.
</action>
<acceptance_criteria>
- `validate_discovery()` returns Chinese errors for missing required fields, bad ids and empty query bundles.
- `discovery_status()` returns counts and missing/blocked/not_run state without mutating files.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `python -m pytest tests/test_discovery.py tests/test_config.py tests/test_skeleton.py -q`
</verification>
