---
phase: "08.1"
plan: "03"
title: Visual CLI, Validation, Documentation And Closeout
status: complete
completed_at: "2026-05-19"
---

# Phase 08.1-03 Summary: Visual CLI, Validation, Documentation And Closeout

## Completed

- 新增 CLI 命令组：
  - `rsa visual extract P001`
  - `rsa visual extract P001 --all-detected`
  - `rsa visual extract P001 --pages 3,5-7`
  - `rsa visual extract P001 --asset-suggestions-only`
  - `rsa visual validate P001`
  - `rsa visual status P001`
- 新增 `_run_visual_command`，统一处理中文输出、expected error 和非 traceback 失败。
- 新增 `visual_status(config, paper_id)`，返回候选总数、cropped、ocr_success、ocr_partial、needs_review、blocked、not_run 计数。
- 更新 README：
  - 核心能力表加入 Visual evidence candidates。
  - 工作流图加入视觉候选层。
  - 安装说明加入 PyMuPDF 基础依赖说明。
  - 常用命令加入 `rsa visual ...`。
  - 新增 Visual Evidence Workflow 小节。
  - 安全边界明确视觉候选只属于 staging/review，不是 formal record。
- 更新 `REQUIREMENTS.md`，标记 V2-VIS-01 至 V2-VIS-05 完成。
- 更新 `ROADMAP.md` 和 `STATE.md`，Phase 8.1 标记完成，Phase 9 成为下一步。

## Safety Boundary

- `validate` 和 `status` 是只读命令，测试会比较文件前后内容。
- CLI 不暴露 formal write，也不会绕过 human confirmation。
- 文档明确 Phase 8.1 不做 AI 评分、LLM 图像结论、曲线还原或高级表格重建。

## Verification

- `python -m pytest tests/test_visual.py tests/test_cli.py tests/test_assets.py tests/test_config.py tests/test_skeleton.py tests/test_templates.py -q`
- `python -m pytest -q`
