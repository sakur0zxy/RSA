---
phase: "08.1"
plan: "01"
title: Visual Candidate Schema And Registry
status: complete
completed_at: "2026-05-19"
---

# Phase 08.1-01 Summary: Visual Candidate Schema And Registry

## Completed

- 新增 `src/rsa_cli/visual.py`，建立 Phase 8.1 视觉证据候选层。
- 定义稳定英文 schema：`candidate_id`、`paper_id`、`asset_id`、`visual_type`、`page`、`region_bbox`、`source_rects`、`source_text`、`confidence`、`evidence_level`、`status`、`source_links`、`advanced_analysis`、`llm_visual_analysis`。
- 新增路径 helper：
  - `visual_candidate_path(config, paper_id)`
  - `visual_context_packet_dir(config, paper_id)`
  - `crop_output_dir(config, paper_id)`
- 新增 registry helper：
  - `load_visual_candidates`
  - `write_visual_candidates`
  - `next_candidate_id`
  - `default_advanced_analysis`
  - `validate_visual_candidate_file`
- 新增 Phase 8 对接 helper：
  - `select_visual_source`
  - `load_asset_suggestions`
  - `build_initial_candidate_from_suggestion`
- 更新 `.gitignore`，允许追踪 `01_literature/assets/*/visual_evidence_candidates.yaml`，继续忽略真实截图、PDF、context packet 和二进制资产。

## Safety Boundary

- `visual_evidence_candidates.yaml` 是 staging/review 文本记录，不是 formal record。
- 真实裁图和 context packet 保持 local-only。
- degraded 状态必须提供中文 `warning_zh` 和 `repair_hint_zh`。
- `advanced_analysis` 默认把曲线提取、表格结构理解、多模态解释都标为 `not_run`。

## Verification

- Covered by `tests/test_visual.py`.
- Registry validation covers duplicate IDs, missing warnings, missing `source_links`, missing `llm_visual_analysis`, and default deferred analysis shape.
