# 17-03 Plan: Tests, Documentation And Audits

## 目标

补齐测试、README、doctor 和 GSD 状态，确保 Phase 17 与当前项目文档和 GitHub 发行说明一致。

## 实施项

- 新增 `tests/test_dynamic_workflow.py`。
- 更新 CLI、config、skeleton、template 测试。
- `rsa doctor` 增加 dynamic workflow policy 检查。
- README 增加动态工作流介绍、命令、产物和边界。
- 更新 ROADMAP、REQUIREMENTS、STATE。
- 执行 health、UAT、milestone、code-review 风格检查并修复发现的问题。

## 验收

- 相关单测通过。
- 全量 pytest 通过。
- `gsd-sdk query state.validate` 和 `validate.health` 通过。
- README 不把动态工作流描述成 formal write 自动执行器。

