---
quick_id: 260513-002
slug: language-policy-audit
status: complete
completed: 2026-05-13
skill: gsd-docs-update
---

# Quick Task 260513-002 Summary: 中文优先语言策略固化与核查

## 完成内容

- 选用 `gsd-docs-update` 的文档核查思路处理本次任务；由于这是小范围规则固化，没有启用子代理或批量重写文档。
- 新增 `.planning/LANGUAGE-POLICY.md`，明确哪些用户可见内容必须中文优先，哪些英文标识必须保持稳定。
- 更新 README、AGENTS、PROJECT 和 REQUIREMENTS，把中文用户优先原则写入当前项目入口。
- 核查已有工作：README、当前 GSD 文档、模板和 formal record seed 基本合规；旧 CLI/profile/config/round 文案有少量英文优先问题，已修正。
- 将 CLI 输出中的 `Profile invalid`、`Round error`、`Configuration error`、`created/existing`、`total/available/blocked`、`passed/report/regressions` 等改为中文优先。

## 风险和边界

- 英文 schema key、CLI flag、状态枚举和代码标识仍保留英文；这是有意保留，避免破坏机器可读接口。
- 历史 phase 执行记录中仍可能保留英文技术描述；这些是归档上下文，不属于当前用户交互面。

## 验证

- `python -m pytest tests/test_cli.py tests/test_profiles.py tests/test_rounds.py tests/test_config.py tests/test_templates.py -q` passed: 43 tests。
- `python -m pytest tests/test_cli.py tests/test_profiles.py tests/test_rounds.py tests/test_config.py tests/test_templates.py tests/test_evals.py -q` passed: 46 tests。
- `python -m pytest -q` passed: 103 tests。
- `python -m rsa_cli.cli --root . eval run` passed，并刷新中文优先的 `eval_report.md`。
- `python -m rsa_cli.cli --root . eval compare` passed: `回归数=0`。
- `git diff --check` 通过；仅出现 Windows LF/CRLF 提示，无空白错误。
